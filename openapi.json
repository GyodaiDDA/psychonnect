{
    "openapi":"3.1.1",
    "info": {
      "title":"Psychonnect",
      "version":"1.0"
    },
    "servers": [
      {
        "url": "#{ENV.fetch('SWAGGER_SCHEME')}://#{ENV.fetch('SWAGGER_HOST')}",
        "description": "Default server 1"
      }
    ],
    "paths": {
    }
  }