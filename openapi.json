{
    "openapi":"3.1.1",
    "info": {
      "title":"Psychonnect",
      "version":"1.0"
    },
    "servers": [
      {
        "url": "#{ENV.fetch('SWAGGER_SCHEME', 'http')}://#{ENV.fetch('SWAGGER_HOST', 'localhost:3000')}",
        "description": "Default server"
      }
    ],
    "paths": {
    }
  }